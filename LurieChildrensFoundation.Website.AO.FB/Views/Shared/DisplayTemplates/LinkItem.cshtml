@using LurieChildrensFoundation._Base.Models.PropertyTypes;
@model FndLinkItemType

@if (Model != null)
{
	<a class="LinkItem" target="@Model.Target" href="@Model.Href" title="@Model.Title">@Model.Text</a>
}
